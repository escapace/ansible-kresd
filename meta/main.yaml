---
galaxy_info:
  role_name: kresd
  description: knot resolver role
  issue_tracker_url: https://github.com/escapace/ansible-kresd/issues
  license: Apache-2.0
  author: escapace
  min_ansible_version: "2.17"

dependencies:
  - role: escapace.epel
  - role: escapace.aws_environment
    when: provider is defined and provider == "ec2"
